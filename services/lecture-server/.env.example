# Server
# Default 4001 to avoid colliding with services/realtime (defaults to 4000)
PORT=4001
# Comma-separated allowlist. Wildcards supported (e.g. https://*.vercel.app)
CLIENT_ORIGIN=http://localhost:3000,https://*.vercel.app

# Redis (for Socket.IO horizontal scaling)
REDIS_URL=redis://localhost:6379

# Supabase (use service role for server-side operations)
SUPABASE_URL=https://avtmohfcixlzriichofq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# LiveKit Cloud
LIVEKIT_URL=wss://your-app.livekit.cloud
LIVEKIT_API_KEY=your_api_key
LIVEKIT_API_SECRET=your_api_secret
